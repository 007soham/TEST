--         Command: mysql
Command Arguments: --host=host --user=user --password=password database < {FILE_ACTIVE}

create database tetdata
  go

  create table table_1(
    ID int primary key;
  )
